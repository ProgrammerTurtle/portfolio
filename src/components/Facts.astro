---
import { facts } from '../data/site';
---
<div class="facts">
  <dl class="wrap">
    {facts.map((f) => (
      <div class="fact">
        <dt>{f.label}</dt>
        <dd class:list={{ plain: f.plain }}>{f.value}</dd>
      </div>
    ))}
  </dl>
</div>
