---
import { facts } from '../data/site';
---
<div class="facts">
  <div class="wrap">
    {facts.map((f) => <div class="fact"><span class="label">{f.label}</span><b>{f.value}</b></div>)}
  </div>
</div>
